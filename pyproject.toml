[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "anvil"
version = "0.1.0"
description = "Autonomous build orchestrator"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["anvil"]
