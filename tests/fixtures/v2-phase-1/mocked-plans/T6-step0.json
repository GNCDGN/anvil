{
  "step_number": 1,
  "step_name": "Create new utility file",
  "files_to_touch": [
    "anvil/utils/hello.py"
  ],
  "operations": [
    "write",
    "smoke-test"
  ],
  "approach": "Create anvil/utils/hello.py with a module-level hello() function returning the string 'hello'. The smoke is a file-existence + grep check (no import), so no __init__.py is required.",
  "smoke_test": "test -f anvil/utils/hello.py && grep -q 'def hello' anvil/utils/hello.py",
  "expected_outcome": "anvil/utils/hello.py exists and exports hello() returning 'hello'.",
  "commit_message": "T6 write-new calibration: create anvil/utils/hello.py",
  "scope_boundaries": {
    "in_scope": "anvil/utils/hello.py only",
    "out_of_scope": "All other files, including anvil/utils/__init__.py"
  },
  "confidence": "high",
  "escalation_triggers": [
    "anvil/utils/ directory conflicts with an existing module of a different shape"
  ]
}
